# Frontend build-time config.
# Copy to .env.local for local overrides, or set as Vercel env vars.

# Backend URL. Leave blank / unset in development to use the Vite proxy.
# In production, set to your Railway (or other) backend URL, no trailing slash.
VITE_API_URL=https://your-backend.up.railway.app
